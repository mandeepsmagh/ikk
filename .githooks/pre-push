#!/bin/sh
# Pre-push hook: check formatting before pushing
# Install: git config core.hooksPath .githooks

cargo fmt --all -- --check
if [ $? -ne 0 ]; then
    echo ""
    echo "Formatting errors found. Run 'cargo fmt --all' to fix."
    exit 1
fi
